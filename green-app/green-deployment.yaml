apiVersion: apps/v1
kind: Deployment
metadata:
  name: green-deployment
  namespace: prod
spec:
  replicas: 3
  selector:
    matchLabels:
      app: my-app
      version: green
  template:
    metadata:
      labels:
        app: my-app
        version: green
    spec:
      containers:
      - name: app
        image: <your-ecr-repo>/green:latest
        ports:
        - containerPort: 3000
